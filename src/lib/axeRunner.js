/**
 * MOVED — do not import this file.
 *
 * axeRunner has been relocated to /api/lib/axeRunner.js.
 * It is server-side only (Playwright + Node APIs) and must never be imported
 * from client-side React code or Vite's module graph.
 *
 * If you are seeing this error at runtime, update your import to:
 *   import { ... } from '../../api/lib/axeRunner'   (from api/scan.js)
 */
throw new Error(
  '[auditV2] axeRunner.js has moved to /api/lib/axeRunner.js. ' +
  'This file is a server-only module and cannot be imported from the client.'
)
